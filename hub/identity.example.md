# Fleet identity — the names you say → repos, domains, data  (TEMPLATE)

Copy this to `identity.md` and fill it in. Jarvis reads `identity.md` alongside `fleet.md` so it can
resolve the brand/product names you actually say to the right repo and the right analytics source —
mappings that aren't obvious from directory names alone. `identity.md` is gitignored (business data).

## <Product name>  — short tagline
- **Repo:** `<repo-dir>` (one level up: `../<repo-dir>`)
- **Domain:** https://<domain>
- **What it is:** <one line>
- **Analytics:** PostHog project "<name>" (id ...) · RevenueCat · GSC property `<domain>`
- ⚠️ Aliases: "<other name>", "<another>" → this product.

## Quick alias table
| You say | Repo(s) | Domain | Analytics |
|---|---|---|---|
| <Brand> | <repo> | <domain> | <sources> |
